---
id: use-theme
title: useTheme
---

`useTheme` is a React hook for retrieving the current global theme.

```ts
const theme = useTheme();

console.log(theme.colors.primary);
```

Syntax:

```ts
function useTheme(): Theme
```
